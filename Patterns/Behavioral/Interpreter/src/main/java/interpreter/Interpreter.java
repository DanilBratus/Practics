package interpreter;

public final class Interpreter {
    public String toLoverCase(String st) {
        return st.toLowerCase();
    }
    public String toUpperCase(String st) {
        return st.toUpperCase();
    }
}
